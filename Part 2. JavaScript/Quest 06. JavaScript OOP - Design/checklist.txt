
Q06
1. 프로토타입 기반 프로그래밍은 객체지향 프로그래밍의 한 형태의 갈래로 클래스가 없고, 클래스 기반 언어에서 상속을 사용하는 것과는 다르게, 객체를 원형(프로토타입)으로 하여 복제의 과정을 통하여 객체의 동작 방식을 다시 사용할 수 있게한다.

2. 프로토타입 객체의 원형이 되는 함수 자체를 말한다.

3. 객체 안에서 var 키워드로 생성할 수 있다.

4. '어떤 객체.prototype = new 다른 객체' 와 같은 식으로 상속을 구현할 수 있다.